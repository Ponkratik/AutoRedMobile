package com.ponkratov.autored.data.di

import org.koin.dsl.bind
import org.koin.dsl.module

val repositoryModule = module {
    /*single {
        FirebaseUserRepositoryImpl(get(), get())
    } bind FirebaseUserRepository::class*/
}